# API configuration settings
server:
  # host: localhost
  host: "dev-app-alb-1479481766.us-east-2.elb.amazonaws.com"
  port: 8000

mimetypes:
  error: ["application/json"]
  json: ["application/json"]
  pdf: ["application/pdf"]
  docx: ["application/vnd.openxmlformats-officedocument.wordprocessingml.document"]

cors:
  enabled: false
  supports_credentials: false
  origins: "*"
  expose_headers:
    - "Content-Disposition"
    - "Content-Type"
    - "Content-Length"

input:
  prefer_file: true

output:
  use_temp_directory: true

logging:
  level: "INFO"
